version: '3.9'

services:
  backend:
    build: .
    environment:
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_RUN_PORT=5000
      - FLASK_APP=backend.app
    ports:
      - "5000:5000"
    volumes:
      - ./backend/storage:/app/backend/storage
